/**
 * Date/platform helpers live in reviews-store.
 * Fake MOCK_REVIEWS / DASHBOARD_STATS removed — UI uses live stored reviews.
 */
export {
  formatReviewDate,
  formatReviewDateShort,
  getPlatformLabel,
} from "@/lib/reviews-store";
